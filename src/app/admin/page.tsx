import Admin from "../users/admin/admin";

// This page serves as the entry point for the Admin dashboard.
// It imports the Admin component from the users/admin directory.
export default function AdminPage() {
  return <Admin />;
}
